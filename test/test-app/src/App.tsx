import { MyComponent } from 'component-library-react';
import React from 'react';

const App: React.FC = () => {
  return <MyComponent first="Stencil" last="'Don't call me a framework' JS" />;
};

export default App;
